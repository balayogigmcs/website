import 'package:flutter/material.dart';

class ResourcesAndTools extends StatelessWidget {
  const ResourcesAndTools({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('Resources and Tools'),
    );
  }
}
